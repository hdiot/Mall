<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_address"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.mebee.mall.activity.AddressManagerActivity">
    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar_addressManage"
        android:layout_alignParentTop="true"
        style="@style/toolbar"
        app:navigationIcon="@drawable/selector_back_icon"
        app:titleTextColor="@color/white"
        app:title="地址管理" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_addrs_addressManage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/toolbar_address"/>

    <Button
        android:id="@+id/btn_add_addressManage"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:text="+新建地址"
        android:background="@drawable/selector_button_square"/>

</RelativeLayout>
